# 更新日志

所有重要的更改都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [v4.15.0] - 2026-03-03

### 修复

- **LLM 工具调用时要求不使用 Markdown**
  - `divine_hexagram` 返回结果中添加 Markdown 禁用提示
  - 避免 LLM 使用 `**粗体**`、`#标题` 等格式

### 问题

当用户通过 LLM 工具调用（如"用算卦工具"）时：
- LLM 收到卦象结果后会重新格式化输出
- 默认使用 Markdown 语法，导致显示问题

### 解决方案

在返回给 LLM 的提示中明确要求：
```
【重要】请使用纯文本格式输出，不要使用任何Markdown语法
```

---

## [v4.14.0] - 2026-03-03

### 修复

- **修复无唤醒词无法触发的问题**
  - 改用 `@filter.event_message_type` 监听所有消息
  - 自己判断是否包含"算卦"或"算一卦"关键词
  - 避免与 `@filter.command` 冲突

### 技术实现

- 使用 `@filter.event_message_type(filter.EventMessageType.ALL)` 监听所有消息
- 检查消息是否以"算卦"或"算一卦"开头
- 检查是否有唤醒词前缀（如 `/`），有则跳过
- 参考 zssm_explain 插件的实现方式

### 触发方式

| 方式 | 示例 | 说明 |
|------|------|------|
| 有唤醒词 | `/算卦` | 使用 `@filter.command` |
| 无唤醒词 | `算卦` | 使用 `@filter.event_message_type` |
| 带问题 | `算卦 今天运势` | 两种方式都支持 |

---

## [v4.13.1] - 2026-03-03

### 修复（四审反馈 - 代码质量）

1. **删除多余的 `import re`**
   - `divine_regex` 方法内部有冗余的局部导入
   - 文件顶部已全局导入 `re` 模块

2. **简化正则匹配逻辑**
   - 装饰器 `@filter.regex` 已匹配消息
   - 方法内部不再重复调用 `re.match`
   - 改用简单字符串操作提取问题

---

## [v4.13.0] - 2026-03-03

### 新增

- **算卦指令支持无唤醒词触发**
  - 直接发送「算卦」或「算一卦」即可触发
  - 无需配置唤醒词前缀（如 `/`）
  - 支持带问题：「算卦 今天工作顺利吗」

### 技术实现

- 使用 `@filter.regex` 装饰器实现无唤醒词触发
- 正则表达式：`^算卦\s*(.*)$|^算一卦\s*(.*)$`
- 重构代码：提取 `_do_divine` 核心方法

### 触发方式对比

| 方式 | 示例 | 说明 |
|------|------|------|
| 有唤醒词 | `/算卦` | 使用 `@filter.command` |
| 无唤醒词 | `算卦` | 使用 `@filter.regex` |
| 带问题 | `算卦 今天运势` | 两种方式都支持 |

---

## [v4.12.0] - 2026-03-02

### 优化（四审反馈 - 代码质量）

1. **提取魔术字符串为常量**
   - 添加 `YANG_LINE = "━━━"` 和 `YIN_LINE = "━ ━"`
   - 所有使用线条字符串的地方改用常量
   - 避免手滑打错带来隐蔽 Bug

2. **PEP 8 规范优化**
   - 添加分隔注释：`# ==================== 常量定义 ====================`
   - 添加分隔注释：`# ==================== 辅助函数 ====================`
   - 添加分隔注释：`# ==================== 插件类 ====================`
   - 提高代码可读性

---

## [v4.11.0] - 2026-03-02

### 修复（四审反馈 - 严重Bug）

1. **缓存键值存取不一致**
   - 存储使用 `unified_msg_origin`，读取使用 `message_id`
   - 导致 Fallback 机制 100% 无法命中缓存
   - 修复：统一使用 `_get_cache_key(event)` 方法

2. **群聊并发缓存覆盖**
   - 同一群聊中不同用户的卦象会互相覆盖
   - 修复：缓存键改为 `{unified_msg_origin}_{sender_id}`
   - 使用 `event.get_sender_id()` 区分用户

### 技术细节

```python
# 新增方法
def _get_cache_key(self, event: AstrMessageEvent) -> str:
    sender_id = event.get_sender_id() or ""
    return f"{event.unified_msg_origin}_{sender_id}"

# 存储和读取都使用统一键
cache_key = self._get_cache_key(event)
self._divination_cache[cache_key] = (...)
cached = self._divination_cache.get(cache_key)
```

---

## [v4.10.0] - 2026-03-02

### 修复（四审反馈）

1. **卦名提取正则更严格**
   - 排除 `【乾卦 · AI解卦】` 等干扰格式
   - 使用 `【([^】·]+?)卦】` 正则

2. **缓存绑定消息ID**
   - 缓存使用 `message_id` 而非 `unified_msg_origin`
   - 避免群聊高频场景下卦象串线

3. **缓存上限控制**
   - 添加 `_max_cache_size = 1000`
   - 超限时删除最旧的条目

4. **输入长度限制**
   - 添加 `_max_question_length = 500`
   - 超限自动截断

5. **异常处理细化**
   - `_load_hexagrams`: 捕获 FileNotFoundError, JSONDecodeError, PermissionError
   - `_load_config`: 捕获 KeyError, TypeError
   - 使用 `logger.exception` 记录未知异常

---

## [v4.9.1] - 2026-03-02

### 优化
- AI解卦指令开始时检测 LLM 是否可用
- 未配置 LLM 时返回友好提示，避免用户等待后才发现错误

---

## [v4.9.0] - 2026-03-02

### 修复（四审反馈）

1. **`算卦` 指令支持命令参数**
   - 添加 `question` 参数，支持 `/算卦 事业` 格式
   - 问题提取优先级：命令参数 > 引用消息

2. **`AI解卦` 优先级调整**
   - 优先从引用内容解析卦象
   - 缓存仅作为引用内容解析失败时的回退
   - 避免多次算卦后引用错配问题

3. **缓存清理策略**
   - 添加 `_cleanup_expired_cache()` 方法
   - 写入缓存时顺带清理过期项
   - 避免缓存无界增长

4. **LLM 超时控制**
   - 使用 `asyncio.wait_for` 添加 120 秒超时
   - 超时后返回友好提示

5. **一致性校验**
   - `_load_hexagrams()` 成功后执行一致性校验
   - 确保懒加载路径也经过校验

---

## [v4.8.0] - 2026-03-02

### 新增
- 引用文本消息算卦时，自动提取文本作为「求卦问题」
- 引用非文本内容（图片/视频/表情包）时，正常算卦但不显示问题

### 对比

| 场景 | AI调用 | 指令调用 |
|------|--------|---------|
| 用户提问 | AI自动提取问题 | 引用文本消息提取问题 |
| 无问题 | 正常算卦 | 正常算卦 |

---

## [v4.7.1] - 2026-03-02

### 优化
- 缓存有效期设置为10分钟
- 算卦输出中提示缓存有效期
- 缓存过期时自动清除

---

## [v4.7.0] - 2026-03-02

### 新增
- 添加算卦结果缓存机制
- AI解卦优先从缓存获取，支持折叠消息和T2I图片

### 修复
- 修复引用折叠消息或T2I图片时无法识别卦象的问题
- 改进错误提示，提供更清晰的解决方案

### 工作原理
1. 算卦时自动缓存结果（基于会话）
2. AI解卦时优先从缓存获取
3. 缓存未命中时尝试从引用内容解析

---

## [v4.6.0] - 2026-03-02

### 修复
- 添加日志输出，确认人格是否被使用
- AI解卦时优先使用会话人格，无人格时使用内置默认提示词

### 移除
- 删除 `AI解卦默认提示词` 配置项（人格优先，无需配置）

### 配置项 (7项)

| 配置项 | 类型 | 说明 |
|--------|------|------|
| 启用变卦 | bool | 是否启用变卦功能 |
| 显示起卦过程 | bool | 是否显示每次抛掷铜钱的结果 |
| 显示爻辞 | bool | 是否显示爻辞 |
| 显示运势指引 | bool | 是否显示运势指引 |
| AI解卦使用T2I | bool | AI解卦结果是否转图片 |
| AI解卦等待提示 | string | 等待提示语，支持 `{卦名}` |
| 显示AI解卦提示 | bool | 是否显示AI解卦提示 |

---

## [v4.5.2] - 2026-03-02

### 修复
- 将配置项的 `value` 字段改为 `default` 字段
- 修复新增配置项默认值为空的问题

---

## [v4.5.0] - 2026-03-02

### 新增配置项

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 显示爻辞 | ✅ 开启 | 是否在卦象结果中显示爻辞 |
| 显示运势指引 | ✅ 开启 | 是否在卦象结果中显示运势指引 |
| AI解卦默认提示词 | (见配置) | 当会话未设置人格时使用的系统提示词 |
| AI解卦等待提示 | (见配置) | AI解卦时显示的等待提示语，支持 `{卦名}` 占位符 |
| 显示AI解卦提示 | ✅ 开启 | 是否在算卦结果末尾显示AI解卦提示 |

### 配置项总览

| 配置项 | 类型 | 说明 |
|--------|------|------|
| 启用变卦 | bool | 是否启用变卦功能 |
| 显示起卦过程 | bool | 是否显示每次抛掷铜钱的结果 |
| 显示爻辞 | bool | 是否显示爻辞 |
| 显示运势指引 | bool | 是否显示运势指引 |
| AI解卦使用T2I | bool | AI解卦结果是否转图片 |
| AI解卦默认提示词 | string | 默认系统提示词 |
| AI解卦等待提示 | string | 等待提示语，支持 `{卦名}` |
| 显示AI解卦提示 | bool | 是否显示AI解卦提示 |

---

## [v4.4.3] - 2026-03-02

### 优化
- AI解卦禁用T2I时，在提示词中要求AI不使用Markdown语法
- 避免纯文本输出时出现Markdown渲染问题

---

## [v4.4.2] - 2026-03-02

### 修复
- 移除不支持的 `use_markdown` 方法调用，修复 AI解卦 报错

---

## [v4.4.0] - 2026-03-01

### 新增
- 添加 `/算卦帮助` 指令，显示插件使用帮助

### 优化
- AI解卦禁用T2I时，同步禁用Markdown语法渲染

### 指令列表

| 主指令 | 别名 | 说明 |
|--------|------|------|
| `[唤醒词]算卦` | `[唤醒词]算一卦` | 生成卦象 |
| `[唤醒词]AI解卦` | `[唤醒词]ai解卦` | AI详细解读 |
| `[唤醒词]卦象 卦名` | `[唤醒词]查卦` | 查询卦象 |
| `[唤醒词]六十四卦` | `[唤醒词]卦列表` | 列出所有卦 |
| `[唤醒词]算卦帮助` | `[唤醒词]卦帮助` `[唤醒词]帮助算卦` | 显示帮助 |

> `[唤醒词]` 默认为 `/`，可在管理面板修改

---

## [v4.3.1] - 2026-03-01

### 优化
- 删除 `/占卜` 别名，简化指令

### 指令列表

| 主指令 | 别名 | 说明 |
|--------|------|------|
| `/算卦` | `/算一卦` | 生成卦象 |
| `/AI解卦` | `/ai解卦` | AI详细解读 |
| `/卦象 卦名` | `/查卦` | 查询卦象 |
| `/六十四卦` | `/卦列表` | 列出所有卦 |

---

## [v4.3.0] - 2026-03-01

### 修复（三审反馈）

#### 1. 卦象线序问题（重要）
- ✅ 统一线序标准为"初爻→上爻"（从下到上）
- ✅ 修正 `TRIGRAM_LINES` 定义顺序
- ✅ 修正 `lines_to_hexagram()` 拼接逻辑
- ✅ 修正 `get_hexagram_display()` 显示逻辑
- 📝 添加详细注释说明线序

#### 2. 日志安全问题
- ✅ 移除人格提示词内容日志
- ✅ 仅记录提示词长度，不泄露内容

#### 3. 指令优化
- ✅ 删除拼音指令（suangua, aijiiegua, guaxiang, liushisigua）
- ✅ 使用中文作为主指令
- ✅ 修复英文指令拼写错误

### 指令列表

| 主指令 | 别名 | 说明 |
|--------|------|------|
| `/算卦` | `/算一卦` `/占卜` | 生成卦象 |
| `/AI解卦` | `/ai解卦` | AI详细解读 |
| `/卦象 卦名` | `/查卦` | 查询卦象 |
| `/六十四卦` | `/卦列表` | 列出所有卦 |

---

## [v4.2.0] - 2026-03-01

### 修复（二审反馈）
- 移除"随机卦回退"逻辑
- 程序化构造 `LINES_TO_TRIGRAM` 映射
- 统一懒加载恢复路径
- 补充严格数据校验

---

## [v4.1.0] - 2026-03-01

### 新增
- 采用传统金钱卦起卦法
- 根据老阳/老阴自然产生变爻

---

## [v4.0.0] - 2026-03-01

### 新增
- 重构插件
- 同时支持指令调用和 LLM 工具调用
